# All optional. When set, the server itself carries the Modal connection
# (deployment mode) and the browser connect screen is skipped. When unset,
# users paste a token on /connect and it is kept in their browser's
# localStorage — nothing is stored server-side.
MODAL_TOKEN_ID=
MODAL_TOKEN_SECRET=
MODAL_ENVIRONMENT=
